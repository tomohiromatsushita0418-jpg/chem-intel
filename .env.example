# ローカル実行用。`cp .env.example .env` して値を埋め、`set -a; . ./.env; set +a` で読み込む。
# AIエンジンはどちらか1つ（Geminiが無料・優先）
GEMINI_API_KEY=AIza...
ANTHROPIC_API_KEY=
COMTRADE_KEY=
DATABASE_URL=
RESEARCH_MODEL=claude-sonnet-4-6
SYNTHESIS_MODEL=claude-opus-4-8
WEB_SEARCH_MAX_USES=8
